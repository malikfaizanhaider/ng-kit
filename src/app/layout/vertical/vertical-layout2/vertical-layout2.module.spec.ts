import { VerticalLayout2Module } from './vertical-layout2.module';

describe('VerticalLayout2Module', () => {
  let verticalLayout2Module: VerticalLayout2Module;

  beforeEach(() => {
    verticalLayout2Module = new VerticalLayout2Module();
  });

  it('should create an instance', () => {
    expect(verticalLayout2Module).toBeTruthy();
  });
});
